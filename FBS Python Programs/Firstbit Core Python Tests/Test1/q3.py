dist_in_km = float(input("Enter the distance in kilometer: "))

dist_in_m = 1000*dist_in_km
dist_in_cm = 100*dist_in_m

print(f"{dist_in_km} km = {dist_in_m} m")
print(f"{dist_in_km} km = {dist_in_cm} cm")